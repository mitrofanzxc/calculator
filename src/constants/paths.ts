interface Ipaths {
  mainFC: string;
  mainCC: string;
  settings: string;
  any: string;
}

const paths: Ipaths = {
  mainFC: '/',
  mainCC: 'maincc',
  settings: 'settings',
  any: '*',
};

export { paths };
