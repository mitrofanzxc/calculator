interface Ipaths {
  mainFC: string;
  mainCC: string;
  settingsFC: string;
  settingsCC: string;
  any: string;
}

const paths: Ipaths = {
  mainFC: '/',
  mainCC: 'maincc',
  settingsFC: 'settingsFC',
  settingsCC: 'settingsCC',
  any: '*',
};

export { paths };
